name: nginx
version: "system"
description: Nginx — links the nginx binary from PATH (brew install nginx / apt install nginx)
platform: linux
arch: all
url: ""
installPath: bin/nginx
binary: nginx
downloadType: system
